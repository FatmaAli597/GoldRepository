import { NIDDirective } from './nid.directive';

describe('NIDDirective', () => {
  it('should create an instance', () => {
    const directive = new NIDDirective();
    expect(directive).toBeTruthy();
  });
});
